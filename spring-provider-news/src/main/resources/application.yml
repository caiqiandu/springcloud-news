server:
  port: 8765
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: springcloud-provider-news
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cqd?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
    thymeleaf:
      encoding: UTF-8
      servlet:
        content-type: text/html
mybatis:
  mapper-locations: classpath:mapper/*.xml

